# ==========================================
# HOTEL BOOKING SYSTEM - OPTIMIZED PRODUCTION DEPENDENCIES
# Removed heavy unused packages to speed up Railway builds
# ==========================================

# --- CORE FLASK FRAMEWORK ---
flask==2.3.3
Flask-Caching>=2.0.0,<3.0.0
gunicorn==21.2.0
Werkzeug>=2.3.0,<3.0.0
Jinja2>=3.1.0,<4.0.0

# --- POSTGRESQL DATABASE ---
psycopg2-binary>=2.9.0
SQLAlchemy>=2.0.0
Flask-SQLAlchemy>=3.0.0

# --- DATA PROCESSING ---
python-dateutil>=2.8.0,<3.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.21.0,<2.0.0

# --- EXCEL/CSV SUPPORT ---
openpyxl>=3.1.0,<4.0.0

# --- OCR FALLBACK (Optional - disabled for faster Railway builds) ---
# opencv-python>=4.8.0,<5.0.0
# pytesseract>=0.3.10,<1.0.0
# Note: OCR is wrapped in try/except blocks in app.py, so it's safe to disable

# --- AI INTEGRATION (GEMINI + OPENROUTER) ---
google-generativeai>=0.3.0,<1.0.0
openai>=1.0.0,<2.0.0

# --- UTILITIES ---
python-dotenv>=1.0.0,<2.0.0
Pillow>=10.0.0,<11.0.0
beautifulsoup4>=4.12.0,<5.0.0
requests>=2.31.0

# --- CHARTS/VISUALIZATION ---
plotly>=5.15.0,<6.0.0

# --- ASYNC HTTP (Required by Railway crawler) ---
aiohttp>=3.9.0,<4.0.0

# --- REMOVED HEAVY DEPENDENCIES (Not actively used) ---
# These packages were removed to speed up Railway builds:
# - selenium (4.15.0) - Web automation not used in production
# - scipy (1.10.0) - Scientific computing not needed
# - psutil (5.9.0) - System utilities not essential
# - firecrawl-py - Web crawling not actively used
# - xlrd - Only openpyxl is sufficient for Excel
# - PyPDF2 - PDF processing not used

# Re-added essential packages:
# - pandas (required by app.py for DataFrame operations)
# - numpy (required by pandas and opencv)
# - opencv-python (required for OCR fallback)
# - pytesseract (required for OCR fallback)
# - plotly (required by dashboard_routes.py for charts)
# - aiohttp (required by optimized_railway_crawler.py)
